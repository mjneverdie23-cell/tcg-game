# SQLite (demo default)
DATABASE_URL="file:./dev.db"

# PostgreSQL (production) — also change `provider` to "postgresql" in prisma/schema.prisma
# DATABASE_URL="postgresql://user:password@localhost:5432/world_atlas"
